# 部署方案（训练中心）

> 架构与运行方案说明；换新电脑部署步骤见 [DEPLOY.md](DEPLOY.md)。
> 活文档：重复踩坑就补规则，每次优化/修复后同步更新本文件、README.md、DEPLOY.md。

## 1. 项目简介

AI 声音训练中心（独立项目，端口 8050），**只负责训练**：训练完自动生成「交付模型」文件夹并清理自己，**不向任何项目自动推送**。为两类下游应用训练模型（完全解耦，各应用自己读自己的模型）：

| 启动方式 | 训练引擎 | 交付到（用户手动复制） |
| --- | --- | --- |
| `启动训练中心-换声.bat` | RVC 换音色 | `交付模型\rvc\<角色名>\`（.pth + .index）→ 复制到 换声应用 `rvc\assets` |
| `启动训练中心-文字驱动.bat` | GPT-SoVITS 文字驱动 TTS | `交付模型\<角色名>\`（ckpt+pth+ref.wav+ref_text.txt 4件套）→ 复制到 文字驱动应用 `tts_service\models\<角色名>\` |

- 两类模型**互不通用**：RVC 模型只会换音色（保留语句/时长）、GPT-SoVITS 模型只会文字朗读；同一角色两种功能都要用，需分别训练两套模型（素材可共用）。
- 训练任务全部完成（含失败）后服务自动退出（`TRAIN_AUTO_EXIT=1`），释放 GPU/主机资源。
- 训练完自动生成交付包 + **自动清理该角色全部中间产物**（work 长期保持很小）。
- 双模式互斥：共用端口 8050，**不要同时开**（训练需独占 GPU/显存）。

## 2. 运行环境与依赖

- 引擎/运行时按「项目内目录布局」放置，路径由代码随脚本位置自动推导（`train_api.py` 的 `PROJECT_ROOT`），复制到任意盘符/目录都能用；特殊部署可用环境变量 `PY312` / `RVC_ROOT` / `GSV_ROOT` / `TRAIN_PORT` 等覆盖（见 DEPLOY.md 环境变量表）。
- NVIDIA GPU 训练（显存建议 ≥8GB），batch/训练量按显存自动适配（≤8G→S1 4/S2 2；8~12G→6/3；≥12G→8/4），8G 卡也能正常训练。
- 引擎为第三方开源软件（RVC-Project/Retrieval-based-Voice-Conversion-WebUI、RVC-Boss/GPT-SoVITS），本仓库只包含对它们的调用封装与少量适配补丁说明，详见 DEPLOY.md 补丁节。

## 3. 目录结构（完整运行目录）

```
xunlianzhongxin/
├─ train_service\train_api.py        主服务（FastAPI，8050，TRAIN_MODE 决定模式）
├─ train_service\index_template.html 页面模板（含任务队列/标字校对 UI；改页面改这里）
├─ runtime\py312\                    便携 Python 3.12 运行时（含 torch，便携版）
├─ runtime\ffmpeg\                   内置 ffmpeg/ffprobe
├─ rvc\                              内置 RVC 引擎（pretrained\ 放 f0G48k/f0D48k 基座）
├─ gptsovits\GPT-SoVITS\             内置 GPT-SoVITS 引擎（pretrained_models\ 放基座）
├─ gptsovits\asr\SenseVoiceSmall\    ASR 标字模型
├─ 交付模型\<角色名>\                 训练完成自动生成交付包 → 手动复制到下游应用
├─ ziliao\xunlianshuju\<角色名>\     数据集目录（放目标人声；目录/文件名用拼音）
├─ output\<角色名>\                  训练产物存档（交付后留一份，便于回看；可手动删）
├─ work\                             训练中间产物（交付后自动清理；素材备份_* 类目录勿删）
├─ tests\                            自测脚本（test_publish_clean.py / test_ref_fix.py）
├─ 启动训练中心-换声.bat / 启动训练中心-文字驱动.bat / 关闭训练中心.bat / start.bat / stop.bat
├─ AGENTS.md / README.md / DEPLOY.md / 部署方案.md
```

## 4. 启动 / 停止

- 训练换声模型：双击 `启动训练中心-换声.bat` → http://127.0.0.1:8050/（页面固定 RVC）
- 训练文字驱动模型：双击 `启动训练中心-文字驱动.bat` → http://127.0.0.1:8050/（页面固定 GPT-SoVITS）
- 统一入口：`start.bat`（菜单选模式）；停止：`关闭训练中心.bat`（按端口 8050 杀进程）或 `stop.bat`
- 训练任务结束服务自动退出（约 5 秒后）；两个模式**不要同时开**
- 启动脚本使用项目内运行时 `runtime\py312\python.exe`，脚本内容为纯 ASCII 英文 + CRLF 换行（勿加中文注释、勿存 UTF-8 BOM、勿改 LF 换行）

## 5. 训练流程（一个角色）

1. 按目标应用选择启动脚本（换声 → RVC；文字驱动 → GPT-SoVITS）
2. 选数据（三种方式任选）：
   - ① 页面点「选择训练文件夹」：浏览器选文件夹上传音频（存 `work\uploaded\<角色名>\`，角色名自动取文件夹名）
   - ② 填「服务器上的文件夹路径」：直读该目录音频（支持 wav/mp3/m4a/flac/aac/ogg；单文件会自动包一层）
   - ③ 放进 `ziliao\xunlianshuju\<角色名>\`：页面不选文件夹、不填路径时默认用这个；可「一键加入全部数据集目录」
3. 填角色名、epochs（填 30 即可，系统自动按目标总步数补足）；方言素材勾选「需要标字校对」
4. 训练前自动素材标准化（`_prep_audio`，ffmpeg）：双声道取人声声道→单声道、滤波降噪、音量归一化约 -18dB、去长静音；产物 `work\prep_<角色>\`，**不改原始素材**（`TRAIN_AUTO_PREP=0` 可关闭）
5. 长音频自动切片；超长切片（>12 秒）自动二次切分为 8 秒段
6. 页面实时显示日志；训练完自动生成 `交付模型\<角色名>\` + 清理该角色中间产物 + 自动退出
7. **手动把交付文件夹复制到下游应用**（见第 6 节），下游应用目录扫描自动识别，重启/刷新即出现

> 素材内容级处理（去 BGM/去混响/去掌声/只保留主要说话人）由素材前置项目完成，本服务不再内置；
> 本服务负责标准级处理（音量/降噪/去长静音/单声道/格式）与切片。

## 6. 模型交付规则

| 模式 | 交付包位置 | 复制到 | 效果 |
| --- | --- | --- | --- |
| 换声（RVC） | `交付模型\rvc\<角色名>\`（`.pth` + `.index`） | 换声应用 `rvc\assets\weights` + `indices` | 换音色，内容/时长保留 |
| 文字驱动（GPT-SoVITS） | `交付模型\<角色名>\`（`.ckpt` + `.pth` + `ref.wav` + `ref_text.txt`） | 文字驱动应用 `tts_service\models\<角色名>\` | 文字→语音 TTS，目录扫描自动识别 |

同时会在本机 `output\<角色名>\` 留一份存档副本。
**训练中心不做任何自动推送**；下游应用均为目录扫描，交付包放进去即识别，无需注册。

### 参考音频（ref.wav / ref_text.txt）规则

- **必须 3~10 秒**：超长参考会导致文字驱动合成漏字/音色跑偏。
- `_make_gpt_ref` 优先选 3~10 秒训练切片；切片超长时**强制裁剪到 8 秒**（SenseVoice ASR 转写文本；ASR 失败也裁剪，绝不发布超长 ref）。
- `_ensure_gpt_ref_ok` 发布前兜底：ref.wav >10 秒强制裁剪到 8 秒并截断 ref_text。
- 自测：`tests\test_ref_fix.py`（155 秒切片 → 8 秒 ref）。

## 7. 任务队列（多角色自动依次训练）

- 页面「任务队列」可添加多个角色（默认数据集目录/服务器路径/上传文件夹），或一键加入全部数据集目录
- API：`POST /api/train_queue`（批量加入）、`POST /api/train_all_datasets`（一键）、`GET /api/queue_status`、`POST /api/queue_clear` / `queue_remove` / `queue_move` / `queue_stop`
- 队列任务一个接一个自动训练，**失败也继续下一个**；队列有任务/正在训练时绝不自动退出
- 上传+队列：`POST /api/train_upload` 带 `queue=1` 只保存不训练，前端随后加入队列

## 8. 标字校对（普通话 + 方言）

- ASR 标字后训练**自动暂停**，页面显示每段切片识别文字供核对修改；普通话识别较准可「跳过校对」，方言（河北话/四川话等）务必逐句校对后再训——这是方言训好的关键环节
- API：`GET /api/review_status`、`POST /api/review_submit`（提交校对后继续）、`POST /api/review_skip`
- 建议：素材 3~10 分钟以上效果较好；单段十几秒的短素材虽能训但效果差

## 9. 训练量 / 批次（可环境变量覆盖）

- 默认：S1 8000 步 + S2 4000 步（`TRAIN_TARGET_STEPS` / `TRAIN_S2_TARGET_STEPS` 覆盖）
- batch 按显存自动选（`_auto_batches`）：≤8G→S1 4/S2 2；8~12G→6/3；≥12G→8/4（`TRAIN_S1_BATCH` / `TRAIN_S2_BATCH` 覆盖）
- S2 注意事项：**已禁用 TF32**（RTX40 系 + cu118 上 S2 起步必崩 0xC0000005，见 DEPLOY.md 补丁 2）；S2 batch 不能大（16G 卡 ≤4、8G 卡 ≤2），否则显存占满卡死

## 10. 验证方法

- `GET /api/health`：返回 mode/engine
- `GET /api/env`：运行模式、交付目录、GPU 显存、批次、基座模型、数据集
- 自测（不跑真实训练）：`runtime\py312\python.exe tests\test_publish_clean.py`（交付包生成 + 清理）、`tests\test_ref_fix.py`（ref 裁剪）
- 训练冒烟：几段短音频跑 2~5 epochs，确认各步骤日志通过、交付模型 + output 有产物、work 中间产物被清理
- 交付后重启下游应用，模型列表出现新角色

## 11. 已知问题与注意

- GPT-SoVITS 训练时间长（数据预处理 + S1/S2 两阶段），耐心等日志
- 训练时尽量别同时跑其他吃显存的服务（8G 卡尤其注意）；系统空闲内存建议 ≥12GB
- 自动退出默认开启；要连续训练多个模型可设 `TRAIN_AUTO_EXIT=0`
- 清理 work 时保留 `work\素材备份_*` 类目录（可能是手动备份的素材）；不删除用户数据集 `ziliao\xunlianshuju\` 下内容
- 基座模型缺失也能训练（从零开始），但质量明显差；建议先在页面点「下载基座模型」或按 DEPLOY.md 手动放置

## 12. 维护约定

- 活文档：重复踩坑就补规则；每次优化/修复后同步更新本文件、README.md、DEPLOY.md
